---
en:
  activerecord:
    models:
      user: {one: "User", other: "Users"}
    attributes:
      user:
        id: User id
        display_id: User id
        email: Email
        password: Password
        password_confirmation: Password confirmation
    errors:
      models:
        user:
          attributes:
            id:
              invalid_characters: can only contain letters and digits
            password:
              insufficient_complexity: needs to contain upper case, lower case and digits
            email:
              invalid_email_address: needs to be a valid email address
  helpers:
    placeholder:
      user:
        display_id: User id
        email: Email
        password: Password
        password_confirmation: Password confirmation

