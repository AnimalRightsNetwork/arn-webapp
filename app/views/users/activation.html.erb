<%= form_for :activation, url: activate_user_path, html: { id: 'activation-form' } do |f| %>
  <%= hidden_field_tag :token, params[:token] %>
  <%= f.submit "Activate account", autofocus: true %>
<% end %>
